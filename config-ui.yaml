apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: ui
  namespace: argocd
  annotations:
    argocd-image-updater.argoproj.io/image-list: "uiservice=docker.io/lexmarkkubernetes/uiservice"
    argocd-image-updater.argoproj.io/apiservice.update-strategy: "latest"  # Or "semver"
spec:
  project: default
  source:
    repoURL: 'https://github.com/lexmarkCS499/UI_Microservice'
    targetRevision: HEAD
    path: 'manifests/overlays/dev'  # Adjust based on your target environment
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
